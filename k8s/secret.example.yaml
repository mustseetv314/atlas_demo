# Example template only. Replace both values before applying and protect the resulting file.
apiVersion: v1
kind: Secret
metadata:
  name: atlas-db-secret
  namespace: atlas
type: Opaque
stringData:
  DB_USERNAME: "replace-with-database-username"
  DB_PASSWORD: "replace-with-database-password"
